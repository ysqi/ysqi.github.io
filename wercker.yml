box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.15" 
        flags: --buildDrafts=false

deploy:
  steps:
    - leipert/add-ssh-key-gh-bb:
        keyname: baidu

    - leipert/git-push:
         host:  https://git.duapp.com
         repo: appid3trdccjj6g
         branch: master
         basedir: public
